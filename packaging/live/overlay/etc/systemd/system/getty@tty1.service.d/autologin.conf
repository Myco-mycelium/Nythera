# Live-demo autologin: the demo session owns tty1 without a password.
# Only shipped in the live image (never in an installed system).
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin demo --noclear %I $TERM
