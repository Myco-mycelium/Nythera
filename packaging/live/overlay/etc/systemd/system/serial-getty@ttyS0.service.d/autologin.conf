# Live-demo autologin on the serial console: the boot smoke (and any
# headless machine) gets the demo session over ttyS0. Only shipped in
# the live image.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin demo --noclear %I 115200,38400,9600 vt100
