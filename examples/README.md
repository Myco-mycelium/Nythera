# examples

Scaffolded per `docs/00-platform/003-ENGINEERING_HANDBOOK.md` (NPC-003 §5).
Intended to hold example applications — one per subsystem where useful
(container manifest authoring, NyFS usage, UI shell apps, gaming/controller
samples) — once there is an implementation to run them against.

Currently empty. The first examples should follow the tutorials in
[`docs/tutorials/`](../docs/tutorials/README.md) so that what a newcomer
reads and what they can run stay in sync.
